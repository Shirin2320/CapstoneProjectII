const { models } = require('../database');

const recipeController = {
    createRecipe: async (userId, recipe) => {

    },
    updateRecipe: async (userId, recipe) => {

    },
    deleteRecipe: async (userId, recipeId) => {
        
    }
}